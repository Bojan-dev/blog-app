// export const saveToLocalStorage = (posts) => {
//   const postsToJSON = JSON.stringify(posts);

//   localStorage.setItem('posts', postsToJSON);
// };

// export const getFromLocalStorage = () => {
//   JSON.parse(localStorage.getItem('posts'));
// };

export default 's';
